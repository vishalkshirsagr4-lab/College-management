# TODO

- [ ] Fix Tailwind styling not working by defining `primary` color in `frontend/tailwind.config.js`.
- [ ] (after fix) Run `npm run dev` in `frontend` and verify `bg-primary`, `border-primary`, and `ring-primary/*` classes apply.

